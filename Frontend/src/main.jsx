import React from "react";
import { createRoot } from "react-dom/client";
import RevMatch from "./RevMatch.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RevMatch />
  </React.StrictMode>
);
